# home-via-nix

基于`home-manager` `Standalone`的`No-NixOS`用户级配置管理, `ansible` 管理system包,github应用,flathub应用,python应用,unfree应用. 开发环境使用`mise`进行管理(本仓库不涉及开发环境的管理).

目前是在Ubuntu26.04上运行, 包含`GNOME`(弃用), `Hyprland`, `niri` 的桌面环境.
